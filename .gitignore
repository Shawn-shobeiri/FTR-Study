# Local / IDE
.DS_Store
.idea/
*.iml

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
venv/
.env
.venv/

# Parquet binaries (large; regenerate with build scripts if needed)
*.parquet
**/*.parquet

# Optional: keep auction_results/ CSV/XML/etc. in repo; exclude parquets only
